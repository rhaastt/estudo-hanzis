# CLAUDE.md

Consulte @AGENTS.md para convenções de arquitetura, comandos, schema de dados e regras de estilo deste projeto.

## Notas específicas do Claude Code

- **Idioma**: código e commits em português
- **Testes**: rodar `node --test 'tests/**/*.test.js'` antes de finalizar mudanças em `js/core/` ou `js/data/`
- **Branch de desenvolvimento**: `claude/eloquent-brown-8QLfa` → push para `main` após aprovação
- **Sem build**: nunca introduzir bundler, transpiler ou gerenciador de pacotes
